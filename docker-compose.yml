version: '3'
services:
  postgres:
    container_name: postgres
    image: postgis/postgis
    ports:
      - '5432:5432'
    volumes:
      - ./postgres_data:/var/lib/postgresql
    env_file:
      - docker.env
    networks:
      - postgres

networks:
  postgres:
    driver: bridge
